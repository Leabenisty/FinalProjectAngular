import { ApitalizeFirstDirective } from './apitalize-first.directive';

describe('ApitalizeFirstDirective', () => {
  it('should create an instance', () => {
    const directive = new ApitalizeFirstDirective();
    expect(directive).toBeTruthy();
  });
});
