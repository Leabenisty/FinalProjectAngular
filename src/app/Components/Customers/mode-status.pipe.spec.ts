import { ModeStatusPipe } from './mode-status.pipe';

describe('ModeStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
